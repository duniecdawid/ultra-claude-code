# Feature Mode — Stage 3: Discuss

Before applying the rules below, read the base rules:
`${CLAUDE_PLUGIN_ROOT}/references/planning-framework/stage-3.md`

The instructions below extend the base rules with feature-mode-specific behavior.
**Precedence:** base first, then extensions. If they conflict, the extension wins.

## Mode Extensions

Feature-mode does not extend Stage 3. The base discussion principles handle feature planning well: surfacing scope risks, time-horizon thinking, RFC creation when an architectural fork appears. Feature planning rarely needs additional discussion structure beyond the base rules.

If you find yourself wanting more structure here in practice, that's a signal to extend this file — don't bypass the base rules silently.

## Stage Transition

When the discussion converges and the user says an explicit proceed phrase (base Exit Protocol), announce:

> **▶ PROCEED TO STAGE 4: WRITE**

Then read:
`${CLAUDE_PLUGIN_ROOT}/skills/feature-mode/references/stage-4.md`
