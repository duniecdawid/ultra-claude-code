# Feature Mode — Stage 4: Write

Before applying the rules below, read the base rules:
`${CLAUDE_PLUGIN_ROOT}/references/planning-framework/stage-4.md`

The instructions below extend the base rules with feature-mode-specific behavior.
**Precedence:** base first, then extensions. If they conflict, the extension wins.

## Mode Extensions

Feature-mode does not extend Stage 4. The base 7-step write flow (scaffold → product docs → architecture → build plan → write README + plan.json → approval gate → post-approval HARD STOP) handles all current feature-planning needs.

If you find yourself wanting to add feature-specific behavior here in practice (e.g., a new task field, a feature-specific docs gate), extend this file — don't sneak the new behavior into the base rules.
